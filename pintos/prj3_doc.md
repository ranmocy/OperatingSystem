# Project 3: Virtual Memory Design Document

Main files are under `vm` directory. They are:

1. frame.h and frame.c
2. page.h and page.c
3. swap.h and swap.c
4. vm.h

Header `vm.h` is just a convinent way to include all other three header files.

In `frame.h` and `frame.c`, we define the
